{
  "name": "dreading-bot-tg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker (cron) that posts the daily reading to a Telegram channel.",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.90.0"
  }
}
